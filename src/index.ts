import { filterNotNull, filterNull, filterPropNotNull } from './filter';
import { mapIfOrElse, mapIfTrue, mapIfFalse } from './map';
import { switchMapIfFalse, switchMapIfOrElse, switchMapIfTrue } from './switchMap'
import { tapIfOrElse, tapError, tapIfTrue, tapIfFalse } from './tap'
import { takeOne } from './take'

export {
    filterNotNull,
    filterNull,
    filterPropNotNull,
    mapIfOrElse,
    mapIfTrue,
    mapIfFalse,
    switchMapIfFalse,
    switchMapIfOrElse,
    switchMapIfTrue,
    tapIfOrElse,
    tapError,
    tapIfTrue,
    tapIfFalse,
    takeOne
};
